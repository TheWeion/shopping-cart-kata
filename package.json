{
  "name": "shopping-cart-kata",
  "version": "1.0.0",
  "description": "A simplified checkout system which tallies up the cost of items stored in a database when queried.",
  "main": "index.js",
  "type": "module",
  "directories": {
    "test": "__tests__"
  },
  "scripts": {
    "start": "node index.js",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --watchAll --silent",
    "coverage": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --coverage --silent"
  },
  "author": "Weion",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.3.1"
  }
}
